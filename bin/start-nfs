#!/bin/sh -eu

mount -t nfsd nfsd /proc/fs/nfsd
chpst -u nfsuser /bin/sh -c "/usr/sbin/exportfs -vifo $EXPORT_SETTINGS $EXPORT_TO:/nfs-share"

/sbin/rpcbind -is
/usr/sbin/rpc.nfsd -d --no-nfs-version 2 --no-nfs-version 3 8 |:
/usr/sbin/rpc.mountd -F

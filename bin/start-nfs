#!/bin/sh -eu

echo "/nfs-share $EXPORT_TO($EXPORT_SETTINGS)" > /etc/exports
/usr/sbin/exportfs -r
/sbin/rpcbind -s
/usr/sbin/rpc.nfsd --no-nfs-version 2 --no-nfs-version 3 8 |:
/usr/sbin/rpc.mountd -F
